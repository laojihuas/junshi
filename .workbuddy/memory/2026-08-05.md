# 2026-08-05 工作日志

## v51 字数压缩 A+B+C(11:05,✅ version 51 已上线)

**背景**:用户反馈军师输出"字数比效多"。诊断出三个放大器:
1. 推拉结构三步指令(先回应→调侃→钩子)逼模型分句输出 → 最大元凶
2. "1-2 句"只管句数不管字数(中文一句可 40+ 字)
3. thinking=max + max_tokens=3000 给了长输出空间;后台"5-25 字"是软表述且被长 system 淹没

**实施(A+B+C 纯提示词,不动参数/架构)**:
- A 硬字数:【自洽与输出要求】"输出 1-2 句" → "整条 ≤30 字,通常 1 句 ≤20 字,最多 2 句合计 ≤30 字;超过=失败必须压缩重写"
- B 推拉压进一句:【语气与态度】推拉结构改为"三步用措辞在同一句完成(回应+调侃+钩子),严禁分句输出;每轮只发 1 句宁缺毋滥"
- C 示例驱动:追加 3 个 ≤20 字范例(含字数标注)

**验证(deploy_v51.mjs,8 场景全绿)**:≤30 字 8/8,平均 20 字;长消息用例(对方 60 字)回复 27 字,"我短她长"执行到位;人味保留(攻击反击/调情/否定/低落共情/挽回期稳住全部达标,attack_detected 逻辑正常)

**注意**:v14 测试输出其实平均 15-25 字已不短,但用户实际使用(带记忆卡/套路/知识库上下文更长)时偏长;v51 的"超过=失败必须重写"硬约束+示例是主要收效手段。后续若仍超标 → D(max_tokens 3000→1200)+ E(长度兜底重试)。

**已提交推送**:955920c(代码+deploy_v51.mjs);PAT 用完即弃未落盘

## v52 S1 口水过滤(11:20,代码完成待部署)

**背景**:用户问"知识库检索到精华也有口水文,怎么让LLM自己挑精华"。给三方案(S1 prompt引导/S2 LLM精筛/S3切块治本),用户只实施 S1。

**改动**(index.ts 参考区注入,零成本零延迟):
- 参考资料指令追加:参考里混有口水文(铺垫/说教/废话/车轱辘话),只吸收"可直接复制的话术/金句/例子",口水段落跳过,不被带偏风格和字数
- 已 commit+push(68caffe);待 PAT 部署

## v52 部署完成(11:22,✅ version 52 已上线)

**验证(deploy_v51.mjs 复用,8 场景)**:≤30 字 8/8,平均 18 字(比 v51 的 20 字还短);人味保留,attack 逻辑正常,长消息场景 22 字"我短她长"。
**观察**:攻击场景本次以"哈哈"开头(如"哈哈 这句话我截图了 以后别打脸"),反击硬度略软于 v51 的"既然你这么自信"——temperature 0.6 正常波动,若用户反馈软可调攻击块语气或降 temp。
**S2(LLM精筛)/S3(切块治本)留待后续**;PAT 用完即弃。

## v53 gem 精华打分(11:48,✅ version 53 已上线,Step 1)

**背景**:用户先问 S2(LLM精筛)是否费 token → 确认不上;问规则打分如何实施 → 决定只做 Step 1(改 index.ts,不碰数据库)。

**实施**:
- `calcGemScore(content,block_title)` 纯规则精华分(零 LLM,范围[-2,3]):
  加分:引号对话示范(≥2对+1.5/1对+0.8)、操作词(你说/不如/试试/回她+0.5)、平均句长≤12字(+0.6)、金句符号→|(+0.4)、块标题含话术/案例(+0.5)
  减分:说教连接词(首先/然后/总之/因此≥3个-1.0)、>30字长句≥2(-0.6)、形容词堆砌≥3(-0.5)、套话≥2(-0.5)
- `recallBlocks`:p_limit 12→24(候选不进 LLM,token 不变)→ 内存 gem → 剔 < -1 → 按 相关分+gem×0.8 重排 → applyQuota 取 target
- _debug 新增 kb_gem/kb_gem_avg;新增 deploy_v53.mjs 验证脚本
- **调参旋钮**:GEM_WEIGHT(0.8)/GEM_MIN(-1) 可随时调;SQL/RPC 未动

**验证(deploy_v53.mjs,5 检索场景)**:gem 分布正常(邀约 [0.9,1.4,-0.6,...],精华块排前,口水块压后);输出质量好(模糊邀约/忽冷忽热安抚/夸细节/不舔收尾/攻击反击)。**注意** -0.6 的块没被剔(未达 GEM_MIN=-1),符合设计。
**遗留**:Step 2(下沉 SQL 省带宽)未做,带宽余量充足无需急。

## 策略徽标前端 UI(12:10,已 commit+push 待帽子云构建)

**背景**:用户问"如何让用户知道 LLM 在打套路,又不暴露给对方,显示在顶部会不会费 token/打乱思路"。分析结论:都不会——_debug 本来就白送 strategy_name/rounds,纯前端 UI 渲染零 token、与 LLM 调用链零交互。用户确认"上"。

**实施(4 文件)**:
- index.html:#chat-strategy 元素(聊天头部好友名下方)
- css/style.css:.chat-strategy 12px 半透明琥珀小字(cs-stage 蓝灰),header 底部内边距 6→10px、标题 top 50%→44% 避让
- js/chat.js:_callIMA 存 this.lastDebug;_updateStrategyBadge 兼容 memory_card(打开会话,已 select * 零请求)与 _debug(回复后)两种输入;三态:有套路→"阶段·策略「name」(rounds/max)"、打断/无→隐藏;try/catch 静默降级
- index.ts:_debug 新增 strategy_max_rounds(进度 x/y 显示,一行)
- **已 commit+push(a9fca1f),前端帽子云自动部署;后端 index.ts 的 strategy_max_rounds 需 PAT 部署 Edge Function(未部署前前端降级显示"第N轮",功能不阻塞)**

## v54 部署完成(12:07,✅ version 54 已上线)

- 部署含 strategy_max_rounds 的 index.ts(策略徽标进度 x/y 完整显示)
- 冒烟 5 场景全绿,输出均 ≤30 字,gem 精排持续生效(邀约 gem=[0.9,1.4,-0.6,...])
- 至此策略徽标功能全链路完成:前端(帽子云已构建)+后端(v54);PAT 用完即弃

## 好友头像关系色 A方案(12:21,✅ 已 commit+push 0ab0d8b 帽子云构建中)

**背景**:用户要求好友列表头像用颜色区分关系阶段(先给了 A/B 两方案对比图,用户选了 A:底色随关系变色);明确形状保持 48×48 方形圆角(border-radius:8px)不动。
**实施(纯前端 2 文件,零 token/零请求)**:
- friends.js:_stageInfo(s) 解析 memory_card.profile.stage(getSessions 已 select *);_STAGE_COLORS: 追求#185FA5/暧昧#993556/恋爱#A32D2D/挽回#854F0B/普通朋友#5F5E5A;未知/无画像→null 保持原 avatar_color;头像加 title="关系阶段:X"
- css/style.css:.friend-avatar 加 transition:background-color 0.3s ease
- 已 commit+push(0ab0d8b);PAT 这次纯前端用不上,未部署后端
- 12:26 修正:没聊天/未知关系头像一律灰色(与普通朋友同色 #5F5E5A,title 显示"未知"),不再保持随机底色;commit 4e377fd

## 长按手动设置关系阶段(13:03,✅ 前端 5ea9557 + 后端 v55 已部署)

**背景**:用户问为什么"追求"显示少(判定保守:extractProfile 无信号标准+没聊过无画像),给出 M3 映射解释后,用户选择先做手动标注(信号标准不用)。
**实施**:
- 前端(3 文件):Action Sheet 加"设置关系"(右侧显示当前阶段+色值);modal-stage 弹层(5 关系选项带色点+恢复AI判断);_setStage 写 memory_card.profile.stage + stage_source='manual';'恢复AI判断'清 manual 标记;头像/Action Sheet 即时刷新
- 后端(index.ts):updateMemoryCard extractProfile 后加 manual 保护——prev.stage_source==='manual' 时强制保留手动 stage+标记(AI 不覆盖);v55 已部署
- 注意:extractProfile 返回对象不含 stage_source,保护分支必须显式回写标记,否则下次 AI 覆盖
- 已 commit+push(5ea9557);PAT 用完即弃

## 剔除登录机制方案评估(14:15,仅方案未动代码)

**用户需求**:剔除邮箱登录 → 设备ID身份 + 三层配额:①免费:新设备前3天50次/天、3-7天30次/天、7天后15次/天,每日清零;②邀请:+50/人(封顶300次),朋友经邀请链接打开且**首次新建好友后**才生效;③激活码:68元/月,绑设备,500次/天×30天,到期回落15次/天;受限即弹付费墙。

**结论:中等工程量(后端2天+前端1天),无架构风险;关键决策=用 Supabase 匿名登录(signInAnonymously)替代邮箱登录**——保住 user_id 主键/RLS(匿名角色即 authenticated)/invite_relations UUID 结构,全部白嫖现有设计;千万别走"纯前端 device_id + service_role 代理"路线(要废 RLS 重写数据层,工作量×3)。

**落地要点**:
- 新增 daily_quota(device_id,day,used_count),扣次用 SECURITY DEFINER 原子函数防并发超扣;档位按 profiles.created_at 天数算
- redeem_invite 改:上限 20 人→6 人(300次)、触发时机=邀请链接首建好友成功(前端 createSession 后调)
- activation_codes 加 used_device_id **绑指纹而非 user id**(匿名 user 清缓存即消失,绑 user id 会炸投诉);业务身份全挂新表 devices.device_id,匿名 user 只是会话载体
- 现有 usage_count 语义混乱(累计消耗/赠送余额混用),借此重构拆成三概念:免费日额度+邀请余额+VIP日额度
- 防刷只能软防(清缓存/无痕/换浏览器),加 IP 日限流兜底;激活码是硬防收入不受影响

**现有资产可复用**:FingerprintJS 指纹(auth.js _getDeviceId)、activate-code、invite-code/redeem、paywall.js、ima-proxy 拦截(改条件为当日额度);付费墙价格 88→68。

## 配额规则补充确认(14:43,产品话术层敲定,IP数值待定)

- **顶部导航**:免费用户不显示任何额度数据;只显示 ①邀请赠送累计次数(不显示 300 上限) ②VIP 剩余天数
- **隐藏信息**:免费档 50/30/15、每日清零、IP 上限、VIP 500次/天上限——均不告知
- **受限文案分层**:免费用户受限→弹付费墙(68元/月);VIP 当日满 500 次→弹"服务过载请明天再试"(不暴露 500);IP 超限→"使用太频繁"(不暴露阈值)
- **IP 每日防刷**:只作用于无 VIP 用户(VIP 豁免,防误伤付费);建议 150 次/天/IP + 可选"同 IP 每日新设备 ≤5";待用户确认数值
- **技术澄清已答复**:设备号=业务身份(激活码/配额/邀请归属),但需匿名登录提供 JWT 通道保住 chat 表 RLS/前端直查;双轨不是绕路,是保住现有数据层

## 剔除登录机制已实施完成(15:40,✅ 后端 v56+SQL 已上线,前端已 push 待帽子云构建)

**用户确认按方案施行**(IP 150/天 + 同 IP 新设备 ≤5;68 元/月)。

**后端(全部部署验证 14/14 PASS)**:
- Dashboard 开启 external_anonymous_users_enabled(匿名登录)
- 008_quota_device.sql:devices/daily_quota/ip_usage 表 + activation_codes.used_device_id + 6 个 SECURITY DEFINER 函数(register_device/check_and_consume_quota/activate_device/redeem_invite_device/get_quota_status/ensure_profile);**表级 GRANT 必给 service_role**(踩坑:invite-code REST 查 devices 报 42501)
- 新函数 device-gate(register/status);activate-code/invite-code/invite-redeem 改设备身份(X-Device-Id);ima-proxy v56:配额 RPC 原子扣次 + 分层 403(reason: quota_exhausted/vip_daily_limit/ip_limit/device_not_found)
- **修复笔误**:activate_device 里 used_by_device_id→used_device_id(列名不一致导致 500)
- 端到端验证:匿名登录→register 幂等→免费档扣次→邀请+50/重复拒→激活码绑设备 30 天→VIP 配额→IP 新设备限流→IP 150 防刷,全绿

**前端(8 文件,已 commit 055375a push)**:
- auth.js 重写:匿名登录+设备注册+Auth.device+URL 邀请码暂存;app.js 去登录分支/auth 页改错误页/去 logout;friends.js 顶部导航只显"邀请赠送X次"+"VIP剩余X天";chat.js X-Device-Id+_handleQuotaBlock 分层弹窗;paywall.js 68 元+设备激活;invite.js redeem(code,deviceId) 时机=首次建好友后;config.js price 68 + device.gateUrl
- **已知限制**:老邮箱账号数据与匿名设备隔离(老用户看不到旧聊天);invite_relations/redeem_invite 旧函数遗留未删
- PAT 用完即弃未落盘

## 配额规则补充确认(14:43,产品话术层敲定,IP数值待定)

## 设备未注册死循环 bug 修复(17:00,✅ commit 6b55086 已 push 待帽子云构建)

**用户反馈**:聊天 20 多次后弹"设备未注册，正在重试"就无法使用。

**根因(代码定位,两层)**:
1. **诱因=device_id 不稳定**:auth.js `_getDeviceId` 依赖 FingerprintJS(jsdelivr CDN 国内时好时坏),加载失败走备用指纹 → visitorId 与 'fp_'+hash 横跳,同一浏览器每次打开都像新设备 → 撞上 register_device 的"同 IP 每日新设备 ≤5"防刷(第 6+ 个新设备被拒)。"20 多次"= 多个 device_id 各自聊的一部分累计。
2. **直接死因=注册失败仍继续使用**:`_registerDevice` 第 44-52 行先无条件把 device_id 写进 Auth.device 再调 device-gate;r.success=false 或 HTTP 非 2xx 时直接 return 不置空 → 前端拿"服务端不存在"的 device_id 反复调 ima-proxy → check_and_consume_quota 每次 device_not_found → 403 → _handleQuotaBlock 无限重试死循环。

**修复(前端 2 文件)**:
- auth.js:device_id 首次生成后写 localStorage('junshi_device_id')固定复用(根治横跳);_registerDevice 返回 boolean,注册失败一律 this.device=null
- chat.js:_checkCanUse 注册失败即拦截提示"设备注册失败请稍后再试";_handleQuotaBlock device_not_found 分支注册失败给明确提示不再无限静默重试(_handleQuotaBlock 改 async)
- 语法校验通过;已 commit+push(6b55086),帽子云自动构建

**遗留**:已卡死用户刷新后若当天该 IP 新设备已满 5 个,注册仍被拒需等次日 IP 计数清零(或查库清 ip_usage.new_devices);后端 ima-proxy device_not_found 自动注册兜底未做(需 PAT 部署,可选)

## 设备未注册 bug 真凶：幽灵计数(17:20,✅ 修复完成)

**用户提供 PAT 查库后真相**：devices 表全库仅 2 行（用户确实只注册 2 台设备，9b53b7 15:10 / 39001f 15:17），但 ip_usage `124.227.4.93` 的 new_devices=5、used=23（≈用户聊的 25 次）。
**根因**：register_device 的 `new_devices` 计数在 INSERT devices **之前** +1 且只增不减；devices 表当日 ins=20/del=18（部署/测试清理删了设备行）→ 计数虚高到 5 → 用户后续换指纹/换设备打开 app 注册被 `ip_new_device_limit` 拦截 → 新指纹未入库 → ima-proxy 每次 `device_not_found` → 前端"设备未注册正在重试"死循环。**计数≠实际注册数，误杀真实用户**。

**修复（三层）**：
1. 线上 SQL：`UPDATE ip_usage SET new_devices=0 WHERE ip='124.227.4.93'`（保留 used_count 防刷）→ 用户设备立即恢复
2. 线上重定义 register_device：+1 移到 INSERT devices **成功之后**（同事务，计数永远=实际新注册数；INSERT 失败/回滚则计数同步回滚）；本地 008_quota_device.sql 同步
3. 前端（commit 6b55086 上一轮）：device_id 写 localStorage 固定复用 + 注册失败置空 device 拦截

**排查经验**：Supabase 日志查询 API（analytics/endpoints/logs.all）免费版字段受限（edge_logs 只有 timestamp/event_message，api_requests 查不到），别依赖日志回溯；直接查 pg_stat_all_tables 的 n_tup_ins/n_tup_del 一眼看出表被删过多少行。PAT 用完即弃。

## v15 时间/位置概念注入(17:45,✅ version 58 已部署)

**背景**:用户反馈军师无时间/位置概念("17点说成8点""下午说成晚上睡不着""三更半夜都能约人")。用户先否掉套路启动改动(新好友首句必启动套路=设计使然:无 stage 时回落默认高频词+三道闸门只过滤"内容存在性"不过滤"适配性",方案1+3留待后续)。

**实施(index.ts,约120行)**:
- `formatCurrentTime()`:Asia/Shanghai(Intl.DateTimeFormat),**小时级稳定**(不显示分钟,同一小时文本相同→保 DeepSeek 前缀缓存);时段显式映射 0-4凌晨/5-6清晨/7-11上午/12-13中午/14-16下午/**17-18傍晚**/19-23晚上;含星期+工作日/周末
- `extractLocation(bio)`:城市词表(约100城)+正则从 profiles.bio 提取(我在/坐标/住/来自/base等),命中才注入,不编造
- buildSystemContent 角色定位后注入【当前时间】+【我的位置】块(基础事实靠前有约束力);_debug 加 now_cn/location
- **缓存设计关键**:时间块放前缀内但小时级稳定(跨小时才 miss 一次);位置块随 bio 固定。已验证 17:01 与 17:59 文本一致

**验证(4+3场景)**:问时间→"五点多了"✓;傍晚说晚不顺着✓;约人结合时间位置✓;location=北京✓;本地 9 组位置提取 9/9 PASS;TS 语法校验通过。
**发现既有问题(与本次无关)**:DeepSeek 偶发失败(3次1次)→ 走 assembleKbReply 知识库拼装降级,把参考文件名+原文直接发给用户,体验差;可后续加重试/超时处理。

**注意**:测试脚本(deploy_time_v15.mjs)注册测试设备后必须删设备+扣回 ip_usage.new_devices(防再次幽灵计数);已 commit+push(8915d6a);PAT 用完即弃。

## v16 套路轮数收紧+优先级降级(18:10,✅ version 59 已部署)

**背景**:用户观察正确——惯例一答一问两三步就完成,但 max_rounds 下限 6 导致套路挂很久;套路指令"优先级高于参考资料"压制精彩话题。

**实施(index.ts,2 处)**:
- A:max_rounds `Math.max(steps.length*2,6)` → `Math.max(steps.length+1,3)`(2步→3轮、3步→4轮)
- B:套路执行指令"本套路决定对话方向,优先级高于参考资料"→"对方抛出更有趣/更投入话题时优先跟随对方,套路自然搁置不强拉回;冲突以对话连续性为准"
- 验证:3 新会话套路启动 max_rounds 均=5(原 6+),回复自然;TS 校验通过;已 commit+push(36cf866)

**其他决策**:手机定位改地点用户决定**不做**(成本:逆地理编码 key 不能放公开仓库需后端代理+前端授权弹窗,金钱 0 元但开发中等、iOS 授权严格、失败回退 bio)。
**令牌位置约定**:用户告知 PAT 存于 `C:/Users/Administrator/Documents/资料.txt`,以后部署自取(只记位置,内容不入日志)。

## 后台统计不准修复(18:20,✅ commit 2d03f7f)

**用户反馈**:admin 后台用户数/总调用次数不准,用户列表不更新,调用次数一直 50。
**查库坐实**:profiles=11 行 vs devices=2 行(匿名登录每次清缓存新建 user+profile 行,profiles≠用户);usage_count 列全 11 行总和=50(旧邮箱时代字段,设备体系上线后不再更新→总调用永远 50);chat_messages assistant=373(真实总调用);daily_quota 今日=42。

**修复**:
- 新增 `admin_stats()` SECURITY DEFINER RPC(009_admin_stats.sql):auth.uid() 鉴权 is_admin,非 admin 返回 {"error":"forbidden"};用户数=devices 行数、调用=daily_quota 累计、历史=msg_assistant、用户列表=设备维度(今日/累计调用+邀请赠送+VIP)
- admin/index.html:loadStats/loadUsers 改调 `sb.rpc('admin_stats')`,表格列改设备维度(7列)
- 已验证:admin token 调 RPC 返回真实数据(user_count=2/today=42/total=42),非 admin forbidden
- **踩坑**:PostgreSQL 无 `row_to_jsonb(record)`,用 `to_jsonb(t)`(row_to_json 返回 json 类型,to_jsonb 返回 jsonb)
- admin_stats 函数返回 forbidden 是 200+error 字段(非 403),前端判断 data.error

**注意**:admin 直查 activation_codes(codes 页) 仍用 anon key(用户没抱怨,未动);is_admin 用户在 profiles 表(laojihua@qq.com)。

## v56 人性化四件套(19:05,代码完成待部署)

**背景**:用户给 IMA copilot 对话实录,发现军师"接不住话外之意,只做字面拆解"(如她哈哈→回"你笑得天气都好了"废话)。要求:意图理解+双关幽默+IOI升级+锚点。
**实施(index.ts,全部提示词层零 token)**:
- ①【先解读再回复】最高优先级(角色定位后):先答三问(真实意图/为什么这么说/期待什么反应)再写;绝不盯字面回字面;哈哈→找笑因强化/推进;正反例 few-shot(用户原话例子)
- ②语气态度加双关幽默:接词第二层意思(放盐→咸淡→生小孩),禁空洞夸赞/直给调侃
- ③【兴趣信号与升级】:IOI(主动追问/发照片/说喜欢/主动约)命中必须推进一档,只接话=浪费信号
- ④【话题锚点】:extractProfile prompt 加 anchor 字段(≤20字);buildSystemContent 有锚点则注入【话题锚点】块;锚点保护(LLM 本轮没识别不清空旧值,防反复丢)
- 新增 deploy_v56.mjs(6 场景:哈哈/随便/太远/放盐/发照片/喜欢小孩,均带构造历史)
- 已 commit+push(935849d);待 PAT 部署验证

## v57 长期记忆 facts(19:15,代码完成待部署)

**背景**:用户问"记忆分长期短期,军师具备了吗"→ 分析:短期(窗口历史/llmHistory近详远略)扎实,长期(memory_card)是雏形:无事实清单/无时间衰减/全量注入不选择性/不能主动教记。用户确认按建议做 P0+P1。
**实施(index.ts)**:
- MemoryCard.facts:[{text≤40字,at,last_mention}] 上限20(FACTS_MAX);注入上限4(FACTS_INJECT_MAX)
- extractProfile 返回 {profile,facts}:prompt 加 facts 提取(≤3条/轮,硬事实:日期/约定/偏好/雷点/家庭/工作/宠物名),挂3分钟限频零新增调用;maxTokens 400→500
- mergeFacts:去重(互含=同条刷新提及时间)+按提及新旧排序+超上限淘汰最旧
- buildSystemContent 选择性注入【我记得这些】:query 与 fact bigram 重叠挑 top4,不相关不注入;factsInjected 进返回值和 _debug
- _debug 新增 facts_len/facts_injected
- 注意:buildSystemContent 返回类型/顶层变量/调用处三处同步改(作用域教训)
- 已 commit+push(8b4cbe5);deploy_v57.mjs 三阶段验证(提取/命中注入/不相关不注入);待 PAT 部署

## 记忆按钮 + v56/v57 部署(19:26,✅ 前端 8329bcc + 后端 v60 已上线)

**背景**:用户要求零成本在聊天右上角加"记忆"按钮查看记忆;PAT 在 C:/Users/Administrator/Documents/资料.txt(部署自取,日志只记位置)。
**后端部署(v60)**:v56(四件套)+v57(facts)一次部署。**重要坑**:代码已升级设备体系(v20260805)——ima-proxy 要求 X-Device-Id 头+check_and_consume_quota,旧脚本全 401 device_required;新 verify_v57.mjs 流程:admin建用户→密码登录→device-gate register→X-Device-Id 调用。
**三阶段验证全绿**:轮1 facts_len=1(生日提取✅)/轮2 facts_injected=1(命中注入✅,回复"看来你已经默认把选择权交给我了"接住但记得感可再强)/轮3 facts_injected=0(不相关不注入✅)。
**前端(3 文件,commit 8329bcc)**:chat-header 右上角盾牌按钮(绝对定位不挤标题)+modal-memory 弹层;openMemoryModal 实时拉最新 memory_card(一次轻量 GET)渲染:关系阶段色点/长期记忆 facts/她的画像(含锚点)/她说过的话/我说过的话;空记忆引导文案;异常降级缓存。
**注意**:资料.txt 存 PAT 是项目约定(记忆里已有),日志不写内容。
