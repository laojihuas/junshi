# 2026-08-05 工作日志

## v51 字数压缩 A+B+C(11:05,✅ version 51 已上线)

**背景**:用户反馈军师输出"字数比效多"。诊断出三个放大器:
1. 推拉结构三步指令(先回应→调侃→钩子)逼模型分句输出 → 最大元凶
2. "1-2 句"只管句数不管字数(中文一句可 40+ 字)
3. thinking=max + max_tokens=3000 给了长输出空间;后台"5-25 字"是软表述且被长 system 淹没

**实施(A+B+C 纯提示词,不动参数/架构)**:
- A 硬字数:【自洽与输出要求】"输出 1-2 句" → "整条 ≤30 字,通常 1 句 ≤20 字,最多 2 句合计 ≤30 字;超过=失败必须压缩重写"
- B 推拉压进一句:【语气与态度】推拉结构改为"三步用措辞在同一句完成(回应+调侃+钩子),严禁分句输出;每轮只发 1 句宁缺毋滥"
- C 示例驱动:追加 3 个 ≤20 字范例(含字数标注)

**验证(deploy_v51.mjs,8 场景全绿)**:≤30 字 8/8,平均 20 字;长消息用例(对方 60 字)回复 27 字,"我短她长"执行到位;人味保留(攻击反击/调情/否定/低落共情/挽回期稳住全部达标,attack_detected 逻辑正常)

**注意**:v14 测试输出其实平均 15-25 字已不短,但用户实际使用(带记忆卡/套路/知识库上下文更长)时偏长;v51 的"超过=失败必须重写"硬约束+示例是主要收效手段。后续若仍超标 → D(max_tokens 3000→1200)+ E(长度兜底重试)。

**已提交推送**:955920c(代码+deploy_v51.mjs);PAT 用完即弃未落盘

## v52 S1 口水过滤(11:20,代码完成待部署)

**背景**:用户问"知识库检索到精华也有口水文,怎么让LLM自己挑精华"。给三方案(S1 prompt引导/S2 LLM精筛/S3切块治本),用户只实施 S1。

**改动**(index.ts 参考区注入,零成本零延迟):
- 参考资料指令追加:参考里混有口水文(铺垫/说教/废话/车轱辘话),只吸收"可直接复制的话术/金句/例子",口水段落跳过,不被带偏风格和字数
- 已 commit+push(68caffe);待 PAT 部署

## v52 部署完成(11:22,✅ version 52 已上线)

**验证(deploy_v51.mjs 复用,8 场景)**:≤30 字 8/8,平均 18 字(比 v51 的 20 字还短);人味保留,attack 逻辑正常,长消息场景 22 字"我短她长"。
**观察**:攻击场景本次以"哈哈"开头(如"哈哈 这句话我截图了 以后别打脸"),反击硬度略软于 v51 的"既然你这么自信"——temperature 0.6 正常波动,若用户反馈软可调攻击块语气或降 temp。
**S2(LLM精筛)/S3(切块治本)留待后续**;PAT 用完即弃。

## v53 gem 精华打分(11:48,✅ version 53 已上线,Step 1)

**背景**:用户先问 S2(LLM精筛)是否费 token → 确认不上;问规则打分如何实施 → 决定只做 Step 1(改 index.ts,不碰数据库)。

**实施**:
- `calcGemScore(content,block_title)` 纯规则精华分(零 LLM,范围[-2,3]):
  加分:引号对话示范(≥2对+1.5/1对+0.8)、操作词(你说/不如/试试/回她+0.5)、平均句长≤12字(+0.6)、金句符号→|(+0.4)、块标题含话术/案例(+0.5)
  减分:说教连接词(首先/然后/总之/因此≥3个-1.0)、>30字长句≥2(-0.6)、形容词堆砌≥3(-0.5)、套话≥2(-0.5)
- `recallBlocks`:p_limit 12→24(候选不进 LLM,token 不变)→ 内存 gem → 剔 < -1 → 按 相关分+gem×0.8 重排 → applyQuota 取 target
- _debug 新增 kb_gem/kb_gem_avg;新增 deploy_v53.mjs 验证脚本
- **调参旋钮**:GEM_WEIGHT(0.8)/GEM_MIN(-1) 可随时调;SQL/RPC 未动

**验证(deploy_v53.mjs,5 检索场景)**:gem 分布正常(邀约 [0.9,1.4,-0.6,...],精华块排前,口水块压后);输出质量好(模糊邀约/忽冷忽热安抚/夸细节/不舔收尾/攻击反击)。**注意** -0.6 的块没被剔(未达 GEM_MIN=-1),符合设计。
**遗留**:Step 2(下沉 SQL 省带宽)未做,带宽余量充足无需急。

## 策略徽标前端 UI(12:10,已 commit+push 待帽子云构建)

**背景**:用户问"如何让用户知道 LLM 在打套路,又不暴露给对方,显示在顶部会不会费 token/打乱思路"。分析结论:都不会——_debug 本来就白送 strategy_name/rounds,纯前端 UI 渲染零 token、与 LLM 调用链零交互。用户确认"上"。

**实施(4 文件)**:
- index.html:#chat-strategy 元素(聊天头部好友名下方)
- css/style.css:.chat-strategy 12px 半透明琥珀小字(cs-stage 蓝灰),header 底部内边距 6→10px、标题 top 50%→44% 避让
- js/chat.js:_callIMA 存 this.lastDebug;_updateStrategyBadge 兼容 memory_card(打开会话,已 select * 零请求)与 _debug(回复后)两种输入;三态:有套路→"阶段·策略「name」(rounds/max)"、打断/无→隐藏;try/catch 静默降级
- index.ts:_debug 新增 strategy_max_rounds(进度 x/y 显示,一行)
- **已 commit+push(a9fca1f),前端帽子云自动部署;后端 index.ts 的 strategy_max_rounds 需 PAT 部署 Edge Function(未部署前前端降级显示"第N轮",功能不阻塞)**

## v54 部署完成(12:07,✅ version 54 已上线)

- 部署含 strategy_max_rounds 的 index.ts(策略徽标进度 x/y 完整显示)
- 冒烟 5 场景全绿,输出均 ≤30 字,gem 精排持续生效(邀约 gem=[0.9,1.4,-0.6,...])
- 至此策略徽标功能全链路完成:前端(帽子云已构建)+后端(v54);PAT 用完即弃
