## 阶段名改名 普通朋友 → 朋友（含头像底色灰→蓝）
- 关系阶段名"普通朋友"在截图里 4 个字（追求/暧昧/恋爱/未知都是 2 个字），用户希望拉齐成 2 个字→改为"朋友"
- 同步要求好友列表头像：追求蓝 / 朋友蓝（其他不变：暧昧粉、恋爱红、挽回琥珀）
- **端到端全链路改名**：
  - 前端：`js/friends.js _STAGE_COLORS '普通朋友':#5F5E5A → '朋友':#185FA5`；`js/chat.js STAGE_COLORS/STAGE_CHAIN/ORDER + 摘除标签注释`；`index.html 设置关系弹窗 stage-option`
  - 后端：`supabase/functions/ima-proxy/index.ts` 改 STAGE_HINTS key/STAGE_ORDER/extractProfile prompt/多处注释（v58 阶段推进升级链）
  - 数据库：`supabase/sql/012_stage_rename_to_friend.sql` 用 `replace(memory_card, '"stage":"普通朋友"', '"stage":"朋友"')`；幂等
  - 部署脚本：`supabase/scripts/deploy_stage_rename.mjs`（一次性，PAT 读取同 vB）+ `deploy_ima.mjs`（保留为后续流）+ `check_stage_counts.mjs`（一次性）
- **执行结果**：
  - SQL 迁移 HTTP 201，残留 0 行
  - 生产数据：朋友（原"普通朋友"）15 行 / 暧昧 12 行 / null_mc 1 / 总 50 — 完美匹配
  - ima-proxy 部署 HTTP 201，**version 63 → 64**
  - 前端文件已 add+commit+push，帽子云自动构建（不必手动重启）
- **前端 push 后用户操作**：刷新浏览器（强刷 Ctrl+Shift+R 绕 sw.js 缓存）才能看到新名字/颜色；老的 memory_card.stage="普通朋友"已被 SQL 改成"朋友"，老用户视觉断点只在 Web 上，旧设备 cookie 还在不会强制重新登录

## 幽灵设备清理（admin "总用户数 7" 实际全是你的）
- 现状调查：devices 表 6 行 + accounts 表 1 行（laojihua 绑 39001f21...）。**4 个 toktest_msg7*_dev 全是测试脚本残留**（IP 124.227.4.93 = 你本机，run_token_test.mjs 等历史跑测没清干净）；39001f21...=你真实 FP；9b53b7721268...=你无痕 FP（IP 不同 168.107.66.15，1 次用量）；**没有外部真实幽灵用户**
- **清理方案（v20260806）**：
  - SQL：`supabase/sql/013_ghost_cleanup.sql` 提供 RPC `cleanup_ghost_devices(p_dry_run)`
    - 规则 A：device_id 命中 `^(toktest_|test_|pytest_|debug_|jstest_)` — 脚本命名，绝无真人用，立即删
    - 规则 B：`fp_*` fallback 设备 + 创建 > 7 天 + 无 invite_code/activation_code + 无 accounts.device_id 绑定 + 无 daily_quota 行 = 漂移孤岛
    - 鉴权：SECURITY DEFINER + 校验 profiles.is_admin（service_role 走 auth.uid() 为空会 forbidden）
  - admin/index.html：用户列表卡加"🧹 清理幽灵"按钮，dry-run 预览（列候选）→ confirm → 真删 → 重新 loadStats
- **执行**：
  - 一次性 SQL DELETE 4 个 toktest_*（service_role 直连，HTTP 201 returning 4 rows）
  - 复查：devices 表剩 2 行（39001f21 + 9b53b...）+ accounts 表 1 行（laojihua），**总用户数应该显示 3（再刷 admin 页就能看到）**
  - 通用部署脚本：`deploy_stage_rename.mjs` 改造成接受文件名参数（`node deploy_stage_rename.mjs 013_xxx.sql`）
  - 调查脚本：`inspect_devices.mjs` / `inspect_accounts.mjs` / `inspect_ghost_candidates.mjs`（一次性，留档作为诊断工具）
  - commit 9c0869c 已推 master
- **改进空间（用户还没决定）**：auth.js 第 156 行 `_signInAnonymously()` 后立即调 `_registerDevice()` → 改成"懒注册"（首次发消息/登录注册/建好友时才 register）。这是个有风险改动（影响 quota 计数/邀请兑现/IP 防刷计数时机），**建议用户先观察一段现在的清理按钮效果，再决定是否动这块**

## v60 主动推进（用户明确需求：军师要主动带关系，不是等对方给信号）
- 用户原话："迷男方法从来不缺乏进攻性…军师利用知识库里千万种技巧话术，一步步把对方拉进更深的恋爱关系"
- **机制改造（supabase/functions/ima-proxy/index.ts，version 64→65 已部署）**：
  - 新增 `ESCALATION_HINTS`（按 stage 的主动推进指令）：未知/朋友→模糊邀约+推拉制造张力（带台阶可退）；追求→半玩笑试探暧昧窗口；暧昧→推拉+模糊邀约守窗口；恋爱→维持浓度+约见面；**挽回→不推进**（'' 空串）
  - 新增 `thisEscalationBlock(stage)`：注入"主动开窗/读反馈再决定下一步/弹药优先用知识库/节奏控制（同手法不连用两轮）"规则；**无目标(goal)或目标已达成时注入**，有目标未达成时仍走 GOAL_HINTS
  - `extractProfile` 阶段判定升级（v60 替换 v58 判定）：升级信号 = ①对方密集兴趣信号 ②**用户主动试探成功（试探/邀约/调侃带钩子后她接住）**；降级 = 用户连续试探她都不接（冷淡/回避/转移/争吵）；正常顺序 朋友→追求→暧昧→恋爱 最多升一级
- **冒烟测试**（smoke_v60.mjs，游客路径：匿名登录→device-gate 注册→带 X-Device-Id 调函数）：朋友→追求自动升档 ✅、KB 命中 3 块 ✅、回复主动带钩子延伸话题 ✅；测试设备 smokev60_* 已清理（cleanup_smoke.mjs）
- **冒烟测试要点（游客身份）**：ima-proxy 需要 X-Device-Id 头（游客）或 X-Identity-Type:account+X-Session-Id（账号）；不带身份头 → 401；测试建会话前要先 device-gate register 否则 device_not_found
- 前端无需改动（toast 升级提示已有）；commit 0b0eb8e 已推 master

## v61 里程碑推进体系（用户产品需求：追求/暧昧阶段要"小目标"逐个拿下）
- 用户需求：关系推进不该只靠 stage 一级级跳，要有抓手——照片/年龄/住址/喜好/家底信息/恋爱经历/敏感面/加微信/约会 9 个里程碑；配合"保持当前关系"选项给用户选择权（停止 or 继续）；**默认（不设目标）从当前阶段一路推进到恋爱**
- **实现（ima-proxy version 65→66 已部署，commit 1928d28）**：
  - `MILESTONE_CHAIN`：9 级里程碑链（先易后难/先公开后私密/先线上后线下）+ `MILESTONE_TIPS` 每级主动引导话术（带台阶可退）
  - `memory_card.milestones: string[]`：已完成里程碑（LLM 在 extractProfile 判定，与已有并集去重、按链序排序）
  - buildSystemContent 注入【关系里程碑】进度块（已完成✓/下一目标/引导话术）：
    - 默认/无目标 → thisEscalationBlock 带下一里程碑目标（战术抓手）
    - goal='保持当前关系' → 停止升级：只显示进度+维持现状（不主动试探/不收集新里程碑）
    - 目标推进中（约见面/推进恋爱/保持暧昧）→ 里程碑作为战术弹药（挽回期不收集）
  - `_debug.milestones` 暴露（前端/验证用）
- **前端**：index.html goal-options 加"保持当前关系"（灰点）+ "不设目标"改名"默认推进（不设目标）"（语义=一路推进到恋爱）；chat.js 记忆页加"推进里程碑"区（✓/○ 九宫格）
- **冒烟实测**（smoke_v60.mjs 更新）：她发照片+说年龄+聊喜好 → milestones=[照片,年龄,喜好] 落库 ✅、stage 朋友→追求 ✅、回复主动带模糊邀约（"没准哪天在一家店碰上了"）✅
- **关键设计**：stage 升级与里程碑互相印证——里程碑=战术动作（本轮拿下什么信息），stage=战略判定（关系到哪一档）；里程碑按链推进不跳级，对方抗拒就换一个过几轮再试

## v61b 里程碑链修正（用户确认排序 + 纠正笔误）
- 用户拍板最终链：**照片 → 年龄 → 喜好 → 住哪 → 家庭 → 恋爱经历 → 敏感面 → 加微信 → 约会**
  - "喜好"提前到"住哪"前（聊天初期即可收集，作邀约弹药）；"住址"→"住哪"；"家底信息"→"家庭"（我之前的笔误）
- **用户明确**：里程碑**不阻塞**关系升级——stage 推进只看兴趣信号（v60 逻辑保持），里程碑只是每轮的"战术抓手"
- 已部署 ima-proxy version 66→67（commit e74a58a），chat.js 九宫格同步；冒烟验证正常（照片/年龄/喜好判定 + stage 追求 + 主动邀约）
- 老数据兼容：旧里程碑词（住址/家底信息）在合并过滤时被 MILESTONE_CHAIN.includes 剔除，无需迁移 SQL（数据量极小）

## v62 一键换话题按钮（用户需求：军师聊偏/答非所问时强制换话题）
- **方案 A 落地**（commit 87909e5，ima-proxy version 67→68 已部署）：
  - 后端：`query='/换话题'` → switchTopic 模式；检索跳过 LLM 拆词（省 token），固定词[新话题/开场白/话题/破冰]+对方聊过兴趣词当方向；buildSystemContent 注入【切换话题】块（**最高优先级覆盖推进**，从记忆卡/锚点/知识库/时间位置抛 ≤30 字新话题开场，不延续旧话题/不道歉/不提元话术）；messages user content 用引导语而非指令；lastUserText 置空跳过攻击检测；_debug.switch_topic
  - 前端：index.html 输入区粘贴按钮旁加 🔄 换话题按钮；chat.js `switchTopic()`（不发 user 消息不污染对话，assistant 落库+渲染+窗口历史，清套路后端处理）；app.js 绑定
- **冒烟实测**：聊偏（工作吐槽）→ 换话题 → 军师抛"你平时在哪个区混 有好吃的店吗"，完全脱离旧话题且命中"住哪"里程碑 ✅
- **交互语义**：换话题 = 一次正常 LLM 调用（扣 1 次 quota）；按钮 loading 期间禁用防连点；返回的 assistant 消息用户复制发给对方

## 使用指导页 guide.html（commit d5bfa7d，纯前端帽子云构建）
- **9 板块移动优先深色单页**：①军师是谁（假装本人+方法论+知识库）②三步用法 ③**第一句话怎么说**（网名+已知信息→军师开场白，2 个示例——onboarding 钩子）④**我的简介使用说明**（✏️ 填名字/年龄/职业/城市，军师扮演你的底稿）⑤关系看得见（**只讲头像颜色=阶段+可设目标，不公开推进/里程碑机制**——用户明确要求核心保密）⑥功能锦囊（换话题/长按/登入）⑦添加到桌面（iOS/安卓 PWA）⑧额度升级（免费/注册/邀请/激活码，不写死数字避免与产品行为打架）⑨安全隐私+FAQ+CTA
- 入口：index.html 顶栏右上方（bio 按钮旁）加 ⓘ 指导按钮 → `location.href='guide.html'`
- **保密红线**：guide.html 严禁出现"里程碑/探测/升级判定/试探成功"等内部机制词（自动化 grep 检查）
- 给用户分享链接 = 域名/guide.html

## guide 联系我们 + 反馈后台（commit 176a2ed，已部署）
- **014_feedback.sql**：feedback 表（content≤2000/contact≤100/status new|done）+ `submit_feedback`（公开 anon 可调，空值/超长拒绝）+ `admin_feedback_list`/`admin_feedback_mark`（SECURITY DEFINER 校验 profiles.is_admin，非管理员 forbidden）
- **guide.html**：用户自己改了文案（第一句话示例改"本人视角"、激活码=包月VIP、FAQ 提示 `/提示：` 前缀）；尾部加"📮 联系我们"按钮 + 弹窗（内容必填/联系方式选填），同步加载 config.js 拿 supabase anonKey 走 REST RPC
- **admin/index.html**：新增"用户反馈"tab——列表（未处理红/已处理绿徽标）+ 一键标记已处理/重新打开 + 刷新；登录后自动加载
- **端到端验证**（verify_feedback.mjs）：匿名提交 200、空内容拒绝、管理员读列表 200、匿名读 forbidden ✅；测试数据已清理
