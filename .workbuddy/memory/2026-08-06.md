## 阶段名改名 普通朋友 → 朋友（含头像底色灰→蓝）
- 关系阶段名"普通朋友"在截图里 4 个字（追求/暧昧/恋爱/未知都是 2 个字），用户希望拉齐成 2 个字→改为"朋友"
- 同步要求好友列表头像：追求蓝 / 朋友蓝（其他不变：暧昧粉、恋爱红、挽回琥珀）
- **端到端全链路改名**：
  - 前端：`js/friends.js _STAGE_COLORS '普通朋友':#5F5E5A → '朋友':#185FA5`；`js/chat.js STAGE_COLORS/STAGE_CHAIN/ORDER + 摘除标签注释`；`index.html 设置关系弹窗 stage-option`
  - 后端：`supabase/functions/ima-proxy/index.ts` 改 STAGE_HINTS key/STAGE_ORDER/extractProfile prompt/多处注释（v58 阶段推进升级链）
  - 数据库：`supabase/sql/012_stage_rename_to_friend.sql` 用 `replace(memory_card, '"stage":"普通朋友"', '"stage":"朋友"')`；幂等
  - 部署脚本：`supabase/scripts/deploy_stage_rename.mjs`（一次性，PAT 读取同 vB）+ `deploy_ima.mjs`（保留为后续流）+ `check_stage_counts.mjs`（一次性）
- **执行结果**：
  - SQL 迁移 HTTP 201，残留 0 行
  - 生产数据：朋友（原"普通朋友"）15 行 / 暧昧 12 行 / null_mc 1 / 总 50 — 完美匹配
  - ima-proxy 部署 HTTP 201，**version 63 → 64**
  - 前端文件已 add+commit+push，帽子云自动构建（不必手动重启）
- **前端 push 后用户操作**：刷新浏览器（强刷 Ctrl+Shift+R 绕 sw.js 缓存）才能看到新名字/颜色；老的 memory_card.stage="普通朋友"已被 SQL 改成"朋友"，老用户视觉断点只在 Web 上，旧设备 cookie 还在不会强制重新登录

## 幽灵设备清理（admin "总用户数 7" 实际全是你的）
- 现状调查：devices 表 6 行 + accounts 表 1 行（laojihua 绑 39001f21...）。**4 个 toktest_msg7*_dev 全是测试脚本残留**（IP 124.227.4.93 = 你本机，run_token_test.mjs 等历史跑测没清干净）；39001f21...=你真实 FP；9b53b7721268...=你无痕 FP（IP 不同 168.107.66.15，1 次用量）；**没有外部真实幽灵用户**
- **清理方案（v20260806）**：
  - SQL：`supabase/sql/013_ghost_cleanup.sql` 提供 RPC `cleanup_ghost_devices(p_dry_run)`
    - 规则 A：device_id 命中 `^(toktest_|test_|pytest_|debug_|jstest_)` — 脚本命名，绝无真人用，立即删
    - 规则 B：`fp_*` fallback 设备 + 创建 > 7 天 + 无 invite_code/activation_code + 无 accounts.device_id 绑定 + 无 daily_quota 行 = 漂移孤岛
    - 鉴权：SECURITY DEFINER + 校验 profiles.is_admin（service_role 走 auth.uid() 为空会 forbidden）
  - admin/index.html：用户列表卡加"🧹 清理幽灵"按钮，dry-run 预览（列候选）→ confirm → 真删 → 重新 loadStats
- **执行**：
  - 一次性 SQL DELETE 4 个 toktest_*（service_role 直连，HTTP 201 returning 4 rows）
  - 复查：devices 表剩 2 行（39001f21 + 9b53b...）+ accounts 表 1 行（laojihua），**总用户数应该显示 3（再刷 admin 页就能看到）**
  - 通用部署脚本：`deploy_stage_rename.mjs` 改造成接受文件名参数（`node deploy_stage_rename.mjs 013_xxx.sql`）
  - 调查脚本：`inspect_devices.mjs` / `inspect_accounts.mjs` / `inspect_ghost_candidates.mjs`（一次性，留档作为诊断工具）
  - commit 9c0869c 已推 master
- **改进空间（用户还没决定）**：auth.js 第 156 行 `_signInAnonymously()` 后立即调 `_registerDevice()` → 改成"懒注册"（首次发消息/登录注册/建好友时才 register）。这是个有风险改动（影响 quota 计数/邀请兑现/IP 防刷计数时机），**建议用户先观察一段现在的清理按钮效果，再决定是否动这块**
