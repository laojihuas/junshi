# 2026-08-07 工作日志

## 记忆卡弹窗滚动修复
- 问题：聊天页右上角记忆卡弹窗内容过多时溢出视口，无法上下滑动
- 原因：`.modal-memory-content` 无 max-height，`.modal-body` 无 overflow-y:auto
- 修复：CSS 加 `max-height:85vh` + `display:flex;flex-direction:column`；body 加 `overflow-y:auto;flex:1 1 auto;min-height:0`；title/footer `flex-shrink:0` 固定
- 已 commit + push（42452cf）

## 套路提炼 v17（灵魂保留）已部署
- 背景：用户反馈套路经常触发但提炼出来不精彩（原 4:1 压缩把节奏/分寸/例句全丢光，只剩骨架）
- 改动（ima-proxy/index.ts）：
  - extractStrategy 输出新增 **essence**（核心原理/节奏/分寸 ≤60字），StrategyState 加可选字段（兼容旧记忆卡）
  - steps 每步强制带**原文例句**（引号原样话术，禁官方腔/翻译腔）+ 时机提示，每步可 50-80 字
  - maxTokens 400→800；temperature 仍 0.2（忠实转录原文例句是低温强项）
  - buildSystemContent 注入 essence（"按此原则临场发挥节奏分寸，不要机械照搬步骤"）+ 例句微调规则
- 部署验证：deploy_strategy_v17.mjs（SBP_PAT 注入），实测 PASS：essence ✓ + steps 含例句 ✓（示例套路"共情转轻松"：essence=先共情再转用自嘲卸情绪节奏要快别硬转；step 带"刚下班就喊累，看来今天是被生活按在地上摩擦了"）
- **踩坑（脚本级）**：REST POST body 必须显式 JSON.stringify，传对象 → PostgREST PGRST102 "Empty or invalid json"（400），导致 session 预建失败、套路写回无处落库（_debug.strategy_name 有值但 SQL 查不到 memory_card）——诊断 2 轮才定位
- 部署版本：ima-proxy v78 ACTIVE；已 commit + push（afd4ac7）
